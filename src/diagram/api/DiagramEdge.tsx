import type { DiagramEdgeProps } from './types'

export function DiagramEdge(_props: DiagramEdgeProps) {
  void _props
  // Declarative-only placeholder collected by DiagramLayout.
  return null
}

DiagramEdge.displayName = 'DiagramEdge'
