import type { DiagramNodeProps } from './types'

export function DiagramNode(_props: DiagramNodeProps) {
  void _props
  // Declarative-only placeholder collected by DiagramLayout.
  return null
}

DiagramNode.displayName = 'DiagramNode'
